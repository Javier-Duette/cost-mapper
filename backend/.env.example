# Conexión a la base de datos.
# SQLite (desarrollo rápido, sin instalar nada):
DATABASE_URL=sqlite:///./costmapper_dev.db

# PostgreSQL (producción / desarrollo avanzado):
# DATABASE_URL=postgresql://user:password@localhost:5432/costmapper
